{
  "name": "alum",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "npm run test:unit",
    "test:unit": "jest --runInBand --testPathPattern=tests/unit",
    "test:integration": "dotenv -e .env.test -- sh -c 'docker compose -f docker-compose.test.yml up -d --wait db_test && npx prisma migrate reset --force --skip-generate --skip-seed && jest --runInBand --setupFilesAfterEnv ./tests/setup.integration.js --testPathPattern=tests/integration'",
    "test:integration:ci": "dotenv -e .env.test -- sh -c 'docker compose -f docker-compose.test.yml down -v >/dev/null 2>&1 || true; docker compose -f docker-compose.test.yml up -d --wait db_test && npx prisma migrate reset --force --skip-generate --skip-seed && jest --runInBand --setupFilesAfterEnv ./tests/setup.integration.js --testPathPattern=tests/integration; EXIT_CODE=$?; docker compose -f docker-compose.test.yml down -v; exit $EXIT_CODE'",
    "prisma": "prisma",
    "dev": "nodemon app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.16.3",
    "bcryptjs": "^2.4.3",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^8.0.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.9",
    "prisma": "^6.16.3",
    "supertest": "^7.1.1"
  }
}
